{
  "main": "server/bin/www",
  "scripts": {
    "start": "cd server && npm start",
    "install": "cd client && npm run install_packages && cd ../server && npm install",
    "build": "cd client && npm run build",
    "dev": "cd server && npm run dev",
    "prod": "cd server && npm run prod"
  }
}
